# Copy to keystore.properties (gitignored) and fill in real values.
# Prefer CI env vars: PALSAFAR_STORE_PASSWORD, PALSAFAR_KEY_PASSWORD, PALSAFAR_KEY_ALIAS, PALSAFAR_STORE_FILE

storeFile=palsafar-release.keystore
storePassword=REPLACE_ME
keyAlias=palsafar-release
keyPassword=REPLACE_ME
